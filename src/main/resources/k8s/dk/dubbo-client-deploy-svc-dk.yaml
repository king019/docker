# webapp-deployment.yaml
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dubbo-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dubbo-consumer
  template:
    metadata:
      labels:
        app: dubbo-consumer
    spec:
      containers:
        - name: dubbo-consumer
          image: docker:5000/dk-king019/dubbo:consumer
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080


# webapp-service.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: dubbo-consumer
spec:
  #  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      #    nodePort: 30001
      protocol: TCP
  selector:
    app: dubbo-consumer